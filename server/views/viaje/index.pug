extends ../layout/index 
block contenido
    main.container.mt-5
        .row
            .col-md-5
                img(src=`/img/destinos_${viaje.imagen}_ln.jpg`, class="img-fluid")
            .col-md-7
                h1 #{viaje.titulo} 
                p Desde el #{viaje.fecha_ida} hasta el #{viaje.fecha_ida} 
                p Valor: $ #{viaje.precio}
                p Cupos: #{viaje.disponibles} personas
                p=viaje.descripcion  
